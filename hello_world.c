#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   // printf("Hello, World!");
   printf("Hello there Russ!");
   printf("\nCreated on Mac and pushed");
   return 0;
}
